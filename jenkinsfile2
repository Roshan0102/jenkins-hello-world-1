pipeline {
    agent any

    tools {
        maven "Maven-3.9"
    }

    stages {
        stage('Check Maven Version') {
            steps {
                sh 'mvn -v'
            }
        }
    }
}
